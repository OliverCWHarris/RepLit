list = []
num_of_scores = int(input('How many scores: '))
for n in range(num_of_scores):
    numbers = int(input('Enter number '))
    list. append(numbers)
    average=sum(list)/num_of_scores
    print(average)